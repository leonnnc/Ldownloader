<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <!--
            usesCleartextTraffic: el servidor suele estar en la red local, por
            HTTP y sin certificado. Sin esto, Android 9+ bloquea la conexión y
            el widget nunca cargaría nada.
        -->

        <!--
            singleTask: el enlace de conexión debe reutilizar la instancia que
            ya existe, no abrir otra. Además es lo que hace que onNewIntent se
            dispare cuando la app estaba en segundo plano.
        -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Enlace de conexión del monitor web: vdl://pair?url=…&token=…
                Es lo que permite configurar la app sin escribir nada. El
                monitor lo ofrece como botón «Configurar la app en este
                teléfono», así que basta con abrir el panel desde el móvil.
                BROWSABLE es obligatorio para que se pueda lanzar desde el
                navegador.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vdl" android:host="pair" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".WidgetProvider"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="net.monitor.descargador.ACTION_WIDGET_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <receiver
            android:name=".ActionReceiver"
            android:exported="false" />
    </application>

</manifest>
